# 📚 Лабораторная работа по Java - ОБОБЩЕННЫЕ ТИПЫ

## 📖 Содержание
1. Общее описание
2. Структура проекта
3. Детальное описание классов
4. Инструкции по запуску

## 1. Общее описание

Проект содержит реализацию четырех практических заданий по функциональному программированию на Java, демонстрирующих работу с обобщенными типами.

## 2. Структура проекта

Проект состоит из 8 основных классов и 3 функциональных интерфейсов:

### Основные классы:
- **Main** - Главный класс с интерактивным меню
- **ListTransformer** - Преобразование элементов списка (задание 1)
- **GenericFilter** - Фильтрация элементов списка (задание 2)
- **Compression** - Агрегация данных (задание 3)
- **CollectionProcessor** - Обработка коллекций (задание 4)

### Функциональные интерфейсы:
- **Function<T, P>** - Для преобразования объектов
- **Filter<T>** - Для фильтрации объектов  
- **CompressionFunction<T>** - Для операций сжатия/агрегации

### Вспомогательные классы:
- **CheckErrors** - Валидация пользовательского ввода

## 3. Детальное описание классов

#### 1. ListTransformer - Преобразование данных
**Принцип работы**: 
- Универсальный метод `transform()` для преобразования элементов списка
- Принимает список и функцию преобразования
- Возвращает новый список с преобразованными элементами

**Демонстрация**:
- **Строки → длины строк**: `"qwerty", "asdfg", "zx"` → `[6, 5, 2]`
- **Числа → модули**: `1, -3, 7` → `[1, 3, 7]`
- **Массивы → максимумы**: `{1,2,3}, {-1,-2,-3}` → `[3, -1]`

#### 2. GenericFilter - Фильтрация данных
**Принцип работы**:
- Универсальный метод `filter()` для фильтрации элементов списка
- Принимает список и функцию-предикат
- Возвращает новый список с отфильтрованными элементами

**Демонстрация**:
- **Фильтрация строк**: длина ≥ 3 → `"qwerty", "asdfg"`
- **Фильтрация чисел**: только положительные → `1, 7`
- **Фильтрация массивов**: без положительных элементов → `{-1,-2,-3}, {-5,0,-10}`

#### 3. Compression - Агрегация данных
**Принцип работы**:
- Универсальный метод `compress()` для агрегации элементов списка
- Принимает список, начальное значение и функцию сжатия
- Возвращает результат агрегации всех элементов

**Демонстрация**:
- **Конкатенация строк**: `"qwerty", "asdfg", "zx"` → `"qwertyasdfgzx"`
- **Суммирование чисел**: `1, -3, 7` → `5`
- **Подсчет элементов**: вложенные списки → общее количество `9`

#### 4. CollectionProcessor - Обработка коллекций
**Принцип работы**:
- Универсальный метод `processCollection()` для сложных операций с коллекциями
- Принимает поставщик коллекции и функцию-аккумулятор
- Поддерживает различные структуры данных (списки, множества, словари)

**Демонстрация**:
- **Разделение чисел**: положительные `[1, 7]` и отрицательные `[-3]`
- **Группировка строк**: по длине `{6: ["qwerty"], 5: ["asdfg"], 2: ["zx", "qw"]}`
- **Уникальные строки**: `"qwerty", "asdfg", "qw"` (дубликаты удалены)

### Функциональные интерфейсы

#### Function<T, P> - Преобразование
**Принцип работы**:
- Единственный метод `apply(T input)`
- Используется для преобразования объектов одного типа в другой

#### Filter<T> - Фильтрация
**Принцип работы**:
- Единственный метод `test(T item)`
- Возвращает boolean - следует ли включать элемент в результат

#### CompressionFunction<T> - Агрегация
**Принцип работы**:
- Единственный метод `compress(T a, T b)`
- Определяет операцию объединения двух элементов

### Вспомогательный класс

#### CheckErrors - Валидация ввода
**Принцип работы**:
- Метод `CheckNumber()` для безопасного ввода чисел
- Обработка `NumberFormatException` при некорректном вводе
- Цикл до получения корректного числового значения


## 4. Пример работы:

- Выберите задание из 3 типа (1-4): 1

1. Отфильтровать строки длиной < 3: “qwerty”, “asdfg”, “zx”
2. Отфильтровать положительные числа: 1, -3, 7  
3. Оставить массивы без положительных элементов: {1,2,3}, {-1,-2,-3}, {0,-1,5}, {-5,0,-10}

- Длины строк: [6, 5, 2]
- Модули чисел: [1, 3, 7]
- Максимальные значения в массивах: [3, -1, 5, 0]

- Выберите задание из 3 типа (1-4): 2

1. Преобразование строк в их длины: “qwerty”, “asdfg”, “zx”
2. Преобразование чисел: отрицательные → положительные, положительные → без изменений: 1, -3, 7
3. Из массивов чисел — максимальные значения каждого массива: {1,2,3}, {-1,-2,-3}, {0,-1,5}, {-5,0,-10}

- Строки длиной ≥ 3: [qwerty, asdfg]
- Положительные числа: [1, 7]
- Массивы без положительных элементов:
[-1, -2, -3]
[-5, 0, -10]

- Выберите задание из 3 типа (1-4): 3

1. Передайте в метод список со значениями: “qwerty”, “asdfg”, “zx”, и сформируйте одну большую строку
2. Передайте в метод список со значениями: 1,-3,7, и верните сумму всех значений исходного списка
3. Имеется список, состоящий из списков целых чисел, получите общее количество элементов во всех списках

1. Объединённая строка: qwertyasdfgzx
2. Сумма чисел: 5
3. Общее количество элементов: 9

- Выберите задание из 3 типа (1-4): 4

1. Разделение чисел на положительные и отрицательные: 1,-3,7
2. Группировка строк по длине: “qwerty”, “asdfg”, “zx”, “qw”
3. Удаление дубликатов строк: “qwerty”, “asdfg”, “qwerty”, “qw”

Задача 1: Положительные и отрицательные числа
Положительные: [1, 7]
Отрицательные: [-3]

Задача 2: Группировка по длине строк
Группы по длине: [[qw, zx], [asdfg], [qwerty]]

Задача 3: Уникальные строки
Уникальные строки: [qwerty, qw, asdfg]

## 5. Инструкции по запуску

### Требования
- Java JDK 8 или выше
- Любая среда разработки (IntelliJ IDEA, Eclipse, VS Code) или командная строка

### Компиляция и запуск

```bash
# Компиляция всех Java файлов
javac *.java

# Запуск главного класса
java Main
